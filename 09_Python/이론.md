# 면접대비
# 컴파일러 vs 인터프리터 차이
# 컴파일러: 자바, 소스를 전체 빌드(컴파일)해서 나중에 실행하는 언어(성능좋음)
#          예) a.java ->(컴파일,빌드)-> a.class (실행)
# 인터프리터: 파이썬, js, 1줄씩 해석+바로실행
#          예) a.py(실행), a.js(실행)

# 크롤링 : 사람이 정보를 탐색하기 보다 자동화해서 html 의 특정 정보를 가져오는 기술
# (스크레이핑도 비슷)
#  기술: request, bs4(뷰티풀숩), 셀레니움을 이용하면 됩니다.
# 1)request: 사이트에 접속해서 html 가져오는 기능
# 2)뷰티풀숩 : 정적사이트(로그인 없는) 태그정보를 가져오는 기능
# 3)셀레니움: 동적사이트 (로그인 있는)태그정보를 가져오는 기능

# flask: 파이썬 웹 서비스 라이브러리
# 특징: 스프링의 컨트롤러처럼 URL을 붙여서 조회/수정/추가/삭제 서비스를 할 수 있음 

# 참고) 프론트 지원 기술면접 : react, vue
#   html 파싱: 
#    (1) html 문서 읽어서: 태그,속성, 글자를 분리해서 각각 트리구조로 메모리에 업로드됩니다.
#       나무(트리를 거꾸로 만든것): 트리 탐색을 통해 가져옵니다.